<section class="container">
  <div class="posts">
    <br>
    <div class="margin">
      <% if flash.any? %>
        <% flash.each do |type, msg| %>
          <div>
            <%= msg %>
          </div>
        <% end %>
      <% end %>
    </div>

    <a class="new_post_link" href="/posts/new">New Post</a>

    <%= render partial: 'posts/post', collection: @posts %>
  </div>
</section>
