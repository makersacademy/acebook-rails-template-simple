<section class="container">
  <div class="posts">
    <div class="margin">
      <% if flash.any? %>
        <% flash.each do |type, msg| %>
          <div>
            <%= msg %>
          </div>
        <% end %>
      <% end %>
    </div>

    <h1 class="margin">News Feed</h1>

    <%= render partial: 'posts/new' %>

    <%= render partial: 'posts/post', collection: @posts %>
  </div>
</section>
