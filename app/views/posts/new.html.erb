<%# This form is now redundant as we are rendering the form in the index page - therefore propose to delete this page (but checking ) %>

<%= form_for @post do |form| %>
  <%= form.label :message %>
  <%= form.text_field :message %>
  <%= form.label :image %>
  <%= form.file_field :image %>

  <%= form.hidden_field :user_id, :value => current_user.id if current_user %>

  <%= form.submit "Submit" %>
<% end %>
