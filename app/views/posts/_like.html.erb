<h4><%= number_of_likes(@post.likes.length)%> </h4>
<% if @post.liked?(current_user) %>
  <%= button_to "Unlike", unlike_path(@post), method: "delete" %>
<% else %>
  <%= button_to "Like", like_path(@post), method: "put" %>
<% end %>
