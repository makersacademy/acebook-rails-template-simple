 <nav class="navbar navbar-expand-lg navbar-light bg-primary" style="background-color: #e3f2fd;">
   <div class="container-fluid">
     <a class=" text-white  navbar-brand" href="/"><%= image_tag('branding/white_logo.png', height: '40') %></a>
     <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent"
       aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
       <span class="navbar-toggler-icon"></span>
     </button>
     <div class="nav navbar-right">
       <div class="collapse navbar-collapse" id="navbarSupportedContent">
         <% if Current.user %>
          <%= button_to 'Logout', logout_path, method: :delete, class: 'btn nav-buttons btn-sm btn-outline-light' %>
         <% else %>
         <%= link_to 'Sign Up', sign_up_path, class: 'btn nav-buttons btn-sm btn-outline-light' %>
          <%= link_to 'Login', sign_in_path, class: 'btn nav-buttons btn-sm btn-outline-light' %>
         <% end %>
       </div>
     </div>
   </div>
 </nav>