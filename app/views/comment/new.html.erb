<%= form_with model: @comment, url: comment_path, local: true do |form| %>

  <%= form.label :comment %>
  <%= form.text_field :content %>
  <%= form.text_field :post_id, value: request.url[-1,1], class:'d-none'%>

  <%= form.submit "Comment" %>
<% end %> 
          
